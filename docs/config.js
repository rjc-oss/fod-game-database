// The one place the repository's name appears in the site. Downloads are fetched from here.
// Change both halves if the repository is ever renamed or moved to another account.
window.FOD_CONFIG = {
  // Where saves/<file> is served from, with a trailing slash.
  rawBase: "https://raw.githubusercontent.com/rjc-oss/fod-game-database/main/saves/",
  // Shown in the footer so a visitor can find the repository itself.
  repoUrl: "https://github.com/rjc-oss/fod-game-database"
};
